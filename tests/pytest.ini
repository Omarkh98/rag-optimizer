[pytest]
python_files = test_*.py
minversion = 6.0
addopts = --strict-markers --tb=short -ra
testpaths = 
    tests
markers =
    asyncio: mark test as asyncio-based for async/await support.
filterwarnings =
    ignore::DeprecationWarning